# Web-Srapper-Basic-
This script prompts the user for a website URL and uses the **Firecrawl API** to extract its core content into clean Markdown format. It then handles any scraping errors gracefully and prints the first 1000 characters of the extracted text to the console.
